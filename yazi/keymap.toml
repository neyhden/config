[mgr]
  prepend_keymap = [
    { on  = "y", run = [ 'shell -- for path in "$@"; do echo "file://$path"; done | wl-copy -t text/uri-list', "yank" ] },
    { on = "<C-n>", run = [ "shell -- dragon-drop -x -i -T %h" ] }
]
